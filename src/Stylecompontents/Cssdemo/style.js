import styled, { css } from 'styled-components'

const temp = css`
  width: 500px;
  height: 500px;
  margin: 120px auto;
  background-color: pink;
`

export const CssdemoWrapper = styled.div`
  ${temp}
`
